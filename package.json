{
	"name": "ez-share",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"engines": {
		"node": "20"
	},
	"scripts": {
		"dev": "vite dev --port 3000",
		"build": "vite build",
		"preview": "vite preview",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"test": "pnpm run test:integration && pnpm run test:unit",
		"test:integration": "playwright test",
		"test:unit": "vitest",
		"lint": "prettier --check .",
		"format": "prettier --write .",
		"deploy:dev": "pnpm build && wrangler pages deploy --branch dev",
		"deploy:prod": "pnpm build && wrangler pages deploy --branch main",
		"cf-typegen": "wrangler types --env preview  && move worker-configuration.d.ts src/",
		"studio": "drizzle-kit studio",
		"db:push": "drizzle-kit push",
		"migrations:generate": "drizzle-kit generate",
		"migrations:run:local": "wrangler d1 migrations apply ez-share-dev --env preview",
		"migrations:run:dev": "wrangler d1 migrations apply ez-share-dev --remote --env preview",
		"migrations:run:prod": "wrangler d1 migrations apply ez-share-prod --remote --env production"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "4.20240529.0",
		"@playwright/test": "1.44.1",
		"@sveltejs/adapter-cloudflare": "4.4.0",
		"@sveltejs/kit": "2.5.10",
		"@sveltejs/vite-plugin-svelte": "3.1.1",
		"drizzle-kit": "0.21.4",
		"prettier": "3.2.5",
		"prettier-plugin-organize-imports": "3.2.4",
		"prettier-plugin-svelte": "3.2.3",
		"prettier-plugin-tailwindcss": "0.6.0",
		"svelte": "5.0.0-next.144",
		"svelte-check": "3.8.0",
		"tslib": "2.6.2",
		"typescript": "5.4.5",
		"unocss": "0.60.3",
		"vite": "5.2.12",
		"vitest": "1.6.0",
		"wrangler": "3.57.2"
	},
	"dependencies": {
		"@unocss/reset": "0.60.3",
		"drizzle-orm": "0.30.10",
		"zod": "3.23.8"
	}
}
